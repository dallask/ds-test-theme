import { gsap } from 'gsap/dist/gsap';
import { ScrollTrigger } from 'gsap/ScrollTrigger';
import { ScrollToPlugin } from 'gsap/ScrollToPlugin';

global.gsap = gsap;
global.ScrollTrigger = ScrollTrigger;
global.ScrollToPlugin = ScrollToPlugin;
